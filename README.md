# Valentine App

Small static Valentine-themed webpage. Open `index.html` in your browser to view.

How to use locally:

```bash
open index.html
```

To push to GitHub (example):

```bash
git remote add origin git@github.com:YOUR_USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```
